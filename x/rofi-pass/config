_rofi () {
    rofi -kb-accept-entry "!Return" "$@"
}

URL_field='url'
USERNAME_field='user'
AUTOTYPE_field='autotype'
delay=3

EDITOR='gvim -f'
BROWSER='xdg-open'

## Misc settings

# default_do options: menu, autotype, copyPass, typeUser, typePass, copyUser, copyUrl, viewEntry, typeMenu, actionMenu, copyMenu, openUrl
default_do='menu'
auto_enter='false'
notify='true'

# color of the help messages
help_color="#e56935"

# Clipboard settings
# Possible options: primary, clipboard, both
clip=both

# Options for generating new password entries
password_length=15

# Custom Keybindings
autotype="Alt+1"
type_user="Alt+2"
type_pass="Alt+3"
open_url="Alt+4"
copy_name="Alt+u"
copy_url="Alt+l"
copy_pass="Alt+p"
show="Alt+o"
copy_entry="Alt+2"
type_entry="Alt+1"
copy_menu="Alt+c"
action_menu="Alt+a"
type_menu="Alt+t"
help="Alt+h"
switch="Alt+x"
